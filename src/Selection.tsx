import React from 'react'

export const Selection: React.FC = () => {
    return (
        <div
            style={{
                pointerEvents: 'none',
                position: 'absolute',
                border: '2px dashed blue',
            }}
        />
    )
}
